<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Movie Recommendation Website</title>
  <link rel="stylesheet" href="styles/form.css">
  <script defer src="scripts/select.js"></script>
</head>

<body>
  <h1>Recommend a Movie!</h1>
  <form id="movie-form" name="movie-form" action="success" method="POST">
    <label>
      Your name
      <input type="text" id="name" name="name" value="<%= data.name %>" />
    </label>
    <label>
      Movie Name
      <input type="text" id="movie" name="movie" value="<%= data.movie %>" />
    </label>
    <label>
      Genre
      <select id="genre" name="genre">
        <option value="action" <%= data.genre === 'action' ? 'selected' : '' %>>Action</option>
        <option value="comedy" <%= data.genre === 'comedy' ? 'selected' : '' %>>Comedy</option>
        <option value="drama" <%= data.genre === 'drama' ? 'selected' : '' %>>Drama</option>
        <option value="sci-fi" <%= data.genre === 'sci-fi' ? 'selected' : '' %>>Sci-Fi</option>
        <option value="horror" <%= data.genre === 'horror' ? 'selected' : '' %>>Horror</option>
        <option value="other" <%= data.genre === 'other' ? 'selected' : '' %>>Other</option>
      </select>
      <input type="text" id="customGenre" name="customGenre" placeholder="Enter the genre" value="<%= data.customGenre || '' %>">
    </label>
    <label>
      What would you rate the movie from 1 - 10?
      <input type="number" id="rating" name="rating" value="<%= data.rating %>" />
    </label>
    <label>
      Why are you recommending the movie?
      <textarea name="why" id="why" rows="5"><%= data.why %></textarea>
    </label>
    <% if (errors) { %>
      <ul class="errors">
        <% errors.forEach((error)=> { %>
          <li>
            <%= error %>
          </li>
          <% }); %>
      </ul>
      <% } %>
        <button type="submit" id="submit">Submit Recommendation</button>
  </form>
  <div class="links">
    <a href="/all-recommendations" class="link-btn">
      See All Recommendations </a>
    <a href="/lookup" class="link-btn">
      Lookup Movies </a>
    <a href="/recommendations" class="link-btn">
      Find Recommendations </a>
  </div>
</body>

</html>